# Test-mode override for VITE_DATA_SOURCE — Vitest resolves Vite env files
# with mode "test" by default, so this takes priority over the developer's
# local .env (which points at the real backend for `npm run dev`). Tests must
# never depend on a live backend being up, so they always run against the
# mock repositories.
VITE_DATA_SOURCE=mock
